skill_name: openclaw
skill_type: remote_execution
base_url: openclaw://arkclaw
auth_type: ssh
init_required: true
required_params:
  - OPENCLAW_HOST
  - OPENCLAW_USERNAME
setup_steps:
  - create_session
execute_mode: remote_command
assert_type: keyword
score_threshold: 0.8
enabled: true
command_template: "{question}"
command_timeout_seconds: 120
